# Required in production: at least 32 random characters.
NOIR_SESSION_SECRET=replace-with-a-64-char-random-secret

# Optional: comma-separated allowed origins for state-changing API requests.
# Example: https://yourdomain.com,https://www.yourdomain.com
NOIR_ALLOWED_ORIGINS=http://localhost:3000
